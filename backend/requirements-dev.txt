# Minimal dependency set to run SentinelAI in DEV_MODE (zero-infra) and CI.
# Excludes heavy/infra-only deps (opensearch, qdrant, kafka, pysigma, langchain,
# sentence-transformers, geoip) which are lazy-imported and optional in DEV_MODE.
fastapi>=0.111
uvicorn[standard]>=0.29
python-multipart>=0.0.9
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20
pydantic>=2.7
pydantic-settings>=2.2
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
bcrypt>=4.0
redis>=5.0
httpx>=0.27
pyyaml>=6.0
structlog>=24.1
groq>=0.9
reportlab>=4.2
python-docx>=1.1
ruff>=0.4
